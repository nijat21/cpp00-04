#include "phonebook.hpp"
#include "contact.hpp"

ContactInfo fill_struct()
{
	while(1)
	{
			}
}


int main(int ac, char **av)
{
	if(av != 1) 
		return 1;
	while(1)
	{
		// if ADD
		// 	prompt for:
		// 		first_name
		// 		last_name
		// 		nickname
		// 		phone
		// 		secret
		// 	if any empty:
		// 		don't add the class
		// 	else:
		// 		add to phonebook
		// if SEARCH
		// 	search and display result
		// if EXIT, return
		// else continue;
	}
}
